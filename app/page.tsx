export default function Home() {
  return (
    <main>
      <h1>API Rate Limit Visualizer</h1>
      <p>
        Week 2 is adapter-focused. Call <code>/api/usage-logs</code> to see
        available providers, then call a provider route to normalize and save
        one usage log row.
      </p>
    </main>
  );
}
